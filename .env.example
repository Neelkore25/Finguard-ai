# Google Gemini API Key (optional - robust rule-based fallback active if not provided)
GEMINI_API_KEY=

# Notification Credentials (optional - graceful in-app alerts active if not configured)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
SENDGRID_API_KEY=

# Database Connection (default: local SQLite)
DATABASE_URL=sqlite:///./finguard.db
